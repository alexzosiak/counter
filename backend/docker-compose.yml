services:
  db:
    image: postgres:15
    container_name: counter_db
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: counter_db
    ports:
      - "5432:5432"